# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# env / secrets — never commit your keys
.env
.env.local
.env.*.local

# runtime discovery snapshots + database (regenerated by the daemons)
data/*.json
data/*.jsonl
data/*.db
data/*.db-shm
data/*.db-wal
!data/.gitkeep

# editor / OS
.DS_Store
*.log
.vercel
